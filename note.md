# 开发备注

## 2026-03-20

### 已接受的边界行为：删除生成中的消息

- 用户允许在消息生成过程中直接删除该消息。
- 删除后，不需要恢复该消息，也不需要把后续生成结果重新写回数据库。
- 删除后，这条消息的后续 REST 查询以数据库状态为准，应表现为 `404 Not Found`。

### 当前已接受的已知限制

- 在极小的并发竞争窗口里，已经建立的 SSE 连接仍可能收到 `message.completed`。
- 但此时数据库中的消息可能已经被删除，因此后续 `GET /api/conversations/:conversation_id/messages/:message_id` 仍会返回 `404`。
- 这不是数据被恢复写回的问题，而是“内存中的流式任务状态”和“数据库最终状态”之间的短暂时序不一致。

### 当前决策

- 以数据库状态为最终事实来源。
- 删除后不做恢复写库。
- 删除后不为 `message not found` / `conversation not found` 这类错误做无意义重试。
